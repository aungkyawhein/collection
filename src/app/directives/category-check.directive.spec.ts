import { CategoryCheckDirective } from './category-check.directive';

describe('CategoryCheckDirective', () => {
  it('should create an instance', () => {
    const directive = new CategoryCheckDirective();
    expect(directive).toBeTruthy();
  });
});
